---
description: Close an EDPA iteration (engine + reports)
allowed-tools: Read, Write, Bash, Grep
model: sonnet
---

# Close Iteration

Run the `edpa-engine` skill to compute derived hours, then `edpa-reports` to generate timesheets and snapshots.
Pass the iteration ID as argument (e.g., "PI-2026-1.3").
