{
  "description": "hooks for tm-pmf — usage tracking",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/log-usage.sh agent"
          }
        ]
      }
    ]
  }
}
