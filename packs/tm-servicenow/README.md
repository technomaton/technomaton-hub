# tm-servicenow

ServiceNow integration: incident management via MCP.

## MCP Server

Requires:
- `SN_INSTANCE` — ServiceNow instance URL
- `SN_USER` — ServiceNow username
- `SN_PASS` — ServiceNow password
